# --- LLM provider --------------------------------------------------------
# HUXForm talks to any provider that speaks the Anthropic Messages API
# or the OpenAI Chat Completions API. Default: Anthropic Claude Opus 4.7.
AGUI_LLM_PROTOCOL=anthropic
AGUI_LLM_BASE_URL=https://api.anthropic.com
AGUI_LLM_MODEL=claude-opus-4-7
AGUI_LLM_API_KEY=your_anthropic_key_here
AGUI_LLM_MAX_TOKENS=4096
AGUI_LLM_TEMPERATURE=0.6

# --- OpenAI-compatible example -------------------------------------------
# AGUI_LLM_PROTOCOL=openai
# AGUI_LLM_BASE_URL=https://api.openai.com/v1
# AGUI_LLM_MODEL=gpt-4o-mini
# AGUI_LLM_API_KEY=sk-...

# --- Optional integrations -----------------------------------------------
# Real web.search via Tavily (otherwise we fall back to a simulated stub).
# TAVILY_API_KEY=tvly-...

# MCP servers config (JSON). See README for the schema.
# AGUI_MCP_CONFIG=.agui/mcp.json

# Persistence + uploaded files live here.
# AGUI_DATA_DIR=.huxform-data

# CLI tools (cli.*) are off by default; enable to wrap host binaries.
# AGUI_ENABLE_CLI=1
# AGUI_CLI_ALLOWLIST=git:gh:jq:curl
